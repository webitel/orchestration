--- 
version: '2'

services:
  backupDB:
    image: mongo:3
    volumes:
      - "${WEBITEL_DIR}/backup:/backup"
    networks:
      - net
    command: "mongodump -h mongo -o /backup/dump" 
    
networks:
  net:
    driver: bridge